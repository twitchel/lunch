{% extends
    (
        app.request.get('min') != null or
        app.request.get('_route') == '_internal' or
        app.request.get('_format') == 'widget'
    ) == true
    ?
    'NinjaLunchBundle::base.min.twig' : (
    app.request.isXmlHttpRequest ? 'NinjaLunchBundle::base.ajax.twig' : 'NinjaLunchBundle::base.html.twig'
    )
%}
