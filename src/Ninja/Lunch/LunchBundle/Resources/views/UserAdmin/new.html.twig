{% extends "NinjaLunchBundle::layout.base.twig" %}
{% block headline %}
Edit item
{% endblock %}
{% block content %}

<h1>User creation</h1>

<form action="{{ path('admin_user_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<div class="record_actions btn-group">
    <a href="{{ path('admin_user') }}" class="btn">
        Back to the list
    </a>
</div>
{% endblock %}

{% block foot_script %}
{{ parent() }}
<script type="text/javascript">
(function($){
    $(document).ready(function(){
        new NinjaLunch.CollectionType($('user_admin_roles_control_group'));;
    });
}(jQuery));
</script>
{% endblock %}
